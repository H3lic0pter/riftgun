# Placeholder node for the 26.1.2 port. Register it in settings.gradle.kts
# (stonecutter versions(...)) and fill in the version coordinates when the
# port starts; only java_version is wired up today.
java_version=25
